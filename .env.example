# My Design Lib — 部署环境变量模板
# 复制为 .env.local 后填入真实值(.env.local 已在 .gitignore 中)
# 使用: set -a; source .env.local; set +a; bash scripts/deploy.sh

SSH_HOST=root@your-server.com
SSH_PORT=22
# 建议使用 SSH key,留空此值即可走 key 认证
SSH_PASS=
DEPLOY_DIR=/var/www/design-lib
CDN_URL=http://your-cdn.example.com
